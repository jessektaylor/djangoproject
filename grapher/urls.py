from django.urls import path

from . import views

urlpatterns = [
    # path('', views.index, name='index'),
    # path('RedditSubsBar', views.RedditSubsBar.as_view()),
    # path('RedditSubsBar/<int:num>/<int:days>', views.RedditSubsBar.as_view(), name='RedditSubBar'),


    # path('', views.FormTest.as_view(),name='FormTest'),
    

]